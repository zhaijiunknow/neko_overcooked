# 构建产物与缓存
# build/ **不忽略**(先生要求入库): 里面就一份编译好的 Overcooked2AI.dll,
# 直接拿来扔进 BepInEx\plugins 就能用, 省掉一次编译环境搭建。
# 代价: 每次重新编译都会让工作区变脏、仓库每次多存一份约 105KB 的二进制
# (Git 存整份新 blob, delta 对这种事帮不上忙) —— 这是有意的取舍。
__pycache__/
*.pyc
.pytest_cache/
pytest-cache-files-*/
Overcooked2AI/obj/
Overcooked2AI/bin/
tools/BepInEx_x86/
tools/ref/
*.log
_sec*.md

# 凭据 / 令牌 —— 绝对不允许进仓库
*令牌*
github_pat_*
.git-credentials*
*.credentials
token.txt

# 反编译得到的游戏源码 —— 属于 Team17 的专有代码, 不随仓库分发。
# 需要的话请用你拥有的正版游戏自行生成, 命令见 README「重新生成反编译源码」。
overcooked_decomp/
overcooked_decomp.7z
overcooked_decomp*.7z
overcooked_decomp*.zip

# 本机工具产物 / 调试快照 —— 不进仓库
.codegraph/
tools/_last_map.txt
shot*.png
